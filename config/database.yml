development:
  adapter: postgresql
  database: band_patron_development
  encoding: utf8
  host: localhost

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.

staging:
  adapter: postgresql
  database: band_patron_staging
  encoding: utf8
  host: localhost

production:
  adapter: postgresql
  database: band_patron_production
  encoding: utf8
  host: localhost

test: &test
  host: localhost
  adapter: postgresql
  encoding: utf8
  database: band_patron_test

cucumber:
  <<: *test

